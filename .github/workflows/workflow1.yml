name: To Show Error

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    name: My First Pipeline
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo "Should fail..." ; exit 1